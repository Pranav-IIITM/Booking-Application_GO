{
  "buildCommand": "cd backend && go build -o server",
  "outputDirectory": "backend",
  "functions": {
    "backend/**/*.go": {
      "runtime": "go"
    }
  },
  "routes": [
    {
      "src": "\/(.*)",
      "dest": "\/server",
      "methods": ["GET", "POST", "OPTIONS", "PUT", "DELETE"]
    }
  ]
}
